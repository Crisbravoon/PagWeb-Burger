import React from 'react';
import ReactDOM from 'react-dom';
import Burger from './Burger';



ReactDOM.render(
  <React.StrictMode>
    <Burger className="fluid"/>
  </React.StrictMode>,
  document.getElementById('root')
);
